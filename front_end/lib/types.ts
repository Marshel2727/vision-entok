export type User = { id: number; username: string; full_name: string; role: "admin" | "operator"; is_active: boolean; last_login_at?: string; created_at: string; updated_at?: string };
export type Detection = { id: number; label: string; confidence: number; is_abnormal: boolean; bbox: { x: number; y: number; width: number; height: number } };
export type DetectionEvent = { id: number; source_type: string; status: string; outcome: "normal" | "abnormal" | "no_detection" | null; max_confidence: number | null; detection_count: number; inference_time_ms: number | null; model_version: string | null; annotated_url: string | null; error_message: string | null; detected_at: string; image: null | { id: number; filename: string; original_filename: string; url: string; width: number; height: number; size_bytes: number }; detections?: Detection[]; review: { status: string; corrected_label?: string | null; notes?: string | null; reviewed_by?: number | null; reviewed_at?: string | null } };
export type ApiResponse<T> = { success: boolean; message: string; data: T; meta: Record<string, unknown> };
